{
  "name": "@miso.ai/server-sdk-monorepo",
  "private": true,
  "workspaces": [
    "packages/server-commons",
    "packages/server-csv",
    "packages/server-feed",
    "packages/server-jobs",
    "packages/server-sdk",
    "packages/server-shopify",
    "packages/server-wordpress"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "version": "node ./bin/version.mjs"
  },
  "devDependencies": {
    "uvu": "^0.5.6"
  }
}
